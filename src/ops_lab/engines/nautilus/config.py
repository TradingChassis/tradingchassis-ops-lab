"""Placeholder Nautilus config models for future slices.

Slice 1 intentionally avoids defining finalized engine configuration schemas.
"""

from pydantic import BaseModel


class NautilusConfigPlaceholder(BaseModel):
    """Temporary marker model for future engine config work."""
